<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="DialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">100%</item> <!-- Ajusta el ancho al 90% de la pantalla -->
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_width">match_parent</item> <!-- Ancho a lo ancho de la pantalla -->
        <item name="android:layout_height">wrap_content</item> <!-- Altura para ajustar al contenido -->
        <item name="android:layout_gravity">bottom</item> <!-- Gravedad para la parte inferior -->
    </style>
    <style name="DialogStyleDate" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">80%</item> <!-- Ajusta el ancho al 90% de la pantalla -->
        <item name="android:windowMinWidthMinor">80%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_width">match_parent</item> <!-- Ancho a lo ancho de la pantalla -->
        <item name="android:layout_height">wrap_content</item> <!-- Altura para ajustar al contenido -->
        <item name="android:layout_gravity">bottom</item> <!-- Gravedad para la parte inferior -->
    </style>
    <style name="CustomDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">100%</item> <!-- Ajusta el ancho al 90% de la pantalla -->
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_width">wrap_content</item> <!-- Ancho ajustado al contenido -->
        <item name="android:layout_height">match_parent</item> <!-- Altura ajustada al contenido -->
        <item name="android:layout_gravity">top</item> <!-- Gravedad para centrar el diálogo en la pantalla -->
    </style>


</resources>